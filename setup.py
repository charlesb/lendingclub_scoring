from setuptools import find_packages, setup

setup(
    name='lendingclub_scoring',
    packages=find_packages(),
    version='0.1.0',
    description='predict if the loans in our dataset are going to be fully repaid',
    author='Charles Bernard',
    license='',
)
